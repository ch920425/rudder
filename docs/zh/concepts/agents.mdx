---
title: "Agents"
description: "Rudder 里的 agent 团队成员和执行方式。"
icon: "bot"
keywords:
  - "AI agent 协作"
  - "agent team"
  - "Rudder"
  - "agent 工作管理"
  - "AI agent 工作流"
canonical: "https://doc.rudder.zeeland.studio/zh/concepts/agents"
"og:url": "https://doc.rudder.zeeland.studio/zh/concepts/agents"

---

在 Rudder 里，agent 是稳定的团队成员。Agent 属于某个组织，有自己的角色、汇报关系、运行时、预算、技能和能力边界。

![Agent 详情界面](../../images/agent-detail.png)

## 什么时候创建 agent

当一类重复工作需要稳定 owner、运行时、技能、预算和汇报线时，再创建 agent。不要为了跑一个还没想清楚的一次性 prompt 就新建 agent；先把工作整理成任务。

## Agent 档案

Agent 记录包含：

- 标题和角色
- 汇报经理和直接下属
- 运行时类型和运行时配置
- 能力描述
- 已配置的预算和执行限制
- 已启用的技能和操作说明

Rudder 用这些字段让 agent 工作可以被分配、评审，并在未来 run 中复用。

## 运行时模型

Rudder 不绑定某一种运行时。它负责安排 agent 工作和记录执行过程；具体怎么理解提示词、调用工具、完成工作，由你选的运行时决定。

Heartbeat 主要有两种执行方式：

- 运行 Rudder 启动并跟踪的本地命令
- 向外部运行时发送请求，由外部环境完成执行

## Heartbeat

Heartbeat 是一次有边界的工作周期。Agent 检查分配给自己的任务，推进工作，然后留下清晰信号：有进展、已完成、被阻塞，或在担任 reviewer 时给出评审意见。

一次运行会保留执行证据：原因、状态、transcript、原始输出、成本、涉及的任务、工作区操作，以及重试或取消记录。

正常循环是：唤醒 agent，检查任务，在配置好的运行时和工作区里行动，然后把证据留回任务上。看板不需要猜发生了什么。

## 汇报线

汇报结构让看板能看清归属、升级和委派。Agent 应该知道自己向谁汇报，也应该知道自己的工作为什么服务于组织目标。

## 下一步

<CardGroup cols={2}>
  <Card title="任务" icon="circle-check" href="/zh/concepts/issues">
    查看 agent 如何接手并关闭持久工作。
  </Card>
  <Card title="技能" icon="book-open" href="/zh/concepts/skills">
    为 agent 打包可复用操作说明。
  </Card>
</CardGroup>
