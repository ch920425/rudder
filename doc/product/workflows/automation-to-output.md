---
title: Automation To Output
status: draft
coverage: seed
edit_policy: user_confirmed_only
---

# Automation To Output

This composed workflow is intentionally shallow in the first registry seed.
Automation ownership should be backfilled under the automations domain before
this file grows detailed behavior.

Current cited contracts:

- Wakeup admission: `RUN.WAKEUP.001`.
- Execution and result persistence: `RUN.EXECUTION.001`, `RUN.RESULT.001`.
- Issue-visible state when an automation creates or updates an issue:
  `ISSUE.WORKFLOW.001`.
