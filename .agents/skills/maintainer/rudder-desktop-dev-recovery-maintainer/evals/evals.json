{
  "skill_name": "rudder-desktop-dev-recovery-maintainer",
  "evals": [
    {
      "id": 1,
      "prompt": "pnpm dev 好像桌面端跑不起来了。你自己试试，别只看 API。",
      "expected_output": "Classifies whether the failure is API, Electron shell, embedded Postgres, or port/process state; reruns the relevant command safely; checks health and Desktop launch evidence; repairs narrowly without touching unrelated dirty files.",
      "files": []
    },
    {
      "id": 2,
      "prompt": "桌面端问题修完之后，发版 0.2.5。",
      "expected_output": "Treats Desktop dev recovery as the blocking first phase, validates the local Desktop path, then explicitly routes the release portion to release-maintainer with the local validation evidence attached.",
      "files": []
    },
    {
      "id": 3,
      "prompt": "debug 一下 update 最新版失败，是不是 release 又坏了？",
      "expected_output": "Separates local update/download/install evidence from remote release state, checks the strongest safe non-dry-run path when the issue is install behavior, and escalates to release-maintainer only if npm/GitHub/tag/assets are actually implicated.",
      "files": []
    },
    {
      "id": 4,
      "prompt": "我本地 Desktop 好像指到错的 Rudder instance 了，页面数据不对。",
      "expected_output": "Verifies the active Desktop base URL, API health, org list, and instance/profile path before diagnosing data, avoids deleting ~/.rudder, and routes to rudder-data-path-diagnostician-maintainer only after the runtime target is pinned down.",
      "files": []
    },
    {
      "id": 5,
      "prompt": "Rudder 更新下载完点 Restart to update 后，旧 app 退出，过一会儿新 app 弹 Electron main process write EPIPE。你先 debug，不要直接说 release 坏了。",
      "expected_output": "Classifies the failure as an update restart / closed progress pipe candidate, records installed app path plus old/new versions, inspects progress JSON/stdout/stderr writer paths and child-process handoff, treats EPIPE or ERR_STREAM_DESTROYED from a closed parent pipe as expected shutdown only when narrowly identified, adds or recommends a regression test that destroys the progress reader before writes or flushes, and finishes with packaged or installed-app verification rather than a dry-run.",
      "files": []
    },
    {
      "id": 6,
      "prompt": "pnpm dev 好像运行不起来了。API health 是 OK，但 Electron 没起来，日志里是 @rudderhq/ui build 的 TypeScript fixture 报错，而且相关文件都是 untracked。",
      "expected_output": "Classifies the blocker as a dirty-WIP UI build/typecheck failure rather than Desktop/Postgres corruption, builds a changed-file ownership packet for the failing fixture and companion untracked files, applies only a safe narrow repair or reports the ownership blocker, reruns the failing build/startup path, and avoids committing a lone file from a broader unrelated feature set.",
      "files": []
    }
  ]
}
